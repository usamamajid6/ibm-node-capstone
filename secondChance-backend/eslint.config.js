module.exports = {
  rules: {
    semi: ['error', 'never'],
    'comma-spacing': ['error', { before: false, after: true }],
    'no-multiple-empty-lines': ['error', { max: 1 }],
    indent: ['error', 2],
    quotes: ['error', 'single'],
    eqeqeq: ['error', 'always'],
  },
}
